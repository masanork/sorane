site:
  title: sorane
  description: OKF-native static site generator
  base_url: https://sorane.pages.dev
  lang: ja

build:
  content_dir: content
  out_dir: dist
  permalink: "{{slug}}.html"
  static_dir: static
  blog:
    archives: false
    tags: false
    featured_mode: off

fonts:
  enabled: false